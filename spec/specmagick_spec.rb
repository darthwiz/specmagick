require 'spec_helper'

describe Specmagick do
  it 'has a version number' do
    expect(Specmagick::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
